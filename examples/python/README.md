# Python examples

```bash
pip install openai anthropic
export TALOS_API_KEY=talos_YOUR_KEY

python openai_sdk.py
python anthropic_sdk.py
```

Both scripts read `TALOS_BASE_URL` and `TALOS_MODEL` too; see the top-level
[`examples/README.md`](../README.md) for defaults.